{%- macro include_optional(sls_file) %}
  {%- for root in opts['file_roots'][saltenv] -%}
    {%- if salt['file.file_exists']('{0}/{1}/init.sls'.format(root, sls_file))  %}
    - {{ sls_file }}
    {% endif %}
  {%- endfor %}
{%- endmacro %}
