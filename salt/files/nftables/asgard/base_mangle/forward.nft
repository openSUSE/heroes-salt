chain forward {
  type filter hook forward priority mangle

  # marks correspond to minor class IDs (1:XX) => pillar/cluster/asgard/network.sls
  iif os-ghr-c oif os-p2p-pub ip6 saddr $net6_ghr-c-vms meta mark set 17
  iifname nat64 oif os-p2p-pub meta mark set 17
}
