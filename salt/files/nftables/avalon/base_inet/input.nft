###################################################
## MANAGED BY SALT in salt/files/nftables/avalon ##
###################################################
{#-
vim:filetype=nftables.jinja
#}

 # input chain for traffic towards the local system (i.e. this firewall itself)
 chain input {
   type filter hook input priority 0
   policy drop

   jump global

  iif vmap {
    os-avalon                      : jump network_os-avalon,
    os-avalon-ct                   : jump network_os-avalon,
    os-p2p-pub                     : jump input_network_os-p2p-pub,
    {%- for network, network_config in pillar['networks']['sites']['slc1'].items() | sort(attribute='1.short') %}
      {%- set shortnet = network_config['short'] %}
    {{ shortnet }} {{ ( ': jump input_network_' ~ shortnet ) | indent(30 - shortnet | length, true) }},
    {%- endfor %}
  }

   iifname nueqsc_stonehat jump input_network_nueqsc
   iifname prg2_asgard jump input_network_prg2

   iif != os-p2p-pub log prefix "[avalon] Inbound Dropped: " flags all
   counter reject
 }

