{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "infra/schemas/disk.json",
	"title": "Disk",
	"description": "Disk size (for standalone hypervisors) or mapped LUN UUID (for HA hypervisor clusters)",
	"type": [ "integer", "string" ],
	"pattern": "^(\\d{1,2}G|[a-z0-9]{33})$"
}
